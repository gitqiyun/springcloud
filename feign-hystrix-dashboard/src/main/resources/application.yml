eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/
spring:
  application:
    name: feign-hystrix-dashboard
server:
  port: 9007
#暴露全部的监控信息
management:
  endpoints:
    web:
      exposure:
        include: "*"